# cnn-vs-ffnn-numpy
CNN vs FFNN implemented from scratch with only NumPy and pandas, compared on Fashion-MNIST across accuracy, parameters, FLOPs, memory, overfitting, data scaling and robustness to rotation, shift and zoom.
